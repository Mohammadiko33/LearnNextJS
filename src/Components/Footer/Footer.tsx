import React from 'react';

interface Props {
  
}

const Page = (props: Props) => {
  return (
    <footer className='bg-black py-6'>
        <h3 className='text-center text-2xl capitalize'>this is footer</h3>
    </footer>
  );
};

export default Page;