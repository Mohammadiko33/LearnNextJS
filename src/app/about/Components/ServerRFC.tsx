import React , {useState} from 'react';

interface Props {}

const ServerRFC = (props: Props) => {
    

  // const [first, setfirst] = useState<string>("") // run app without error /"use client"/ error 

  return (
    <div>ServerRFC</div>
  );
};

export default ServerRFC;